import styledComponents from 'styled-components'

export const styled = styledComponents

export { css, keyframes, createGlobalStyle, ServerStyleSheet } from 'styled-components'
export * from 'polished'
