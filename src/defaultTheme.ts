import { createTheme } from '@flame-ui/theme'

export const {
  theme,
  colorKeys,
  fontKeys,
  fontSizeKeys,
  lineHeightKeys,
  letterSpacings,
} = createTheme({
  colors: {
    primary: '#2672FF',
    red: '#ff6262',
  },
})
